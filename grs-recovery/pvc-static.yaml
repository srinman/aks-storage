apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-blob-grs-static
  namespace: blob-demo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-blob-grs-static
  storageClassName: blob-fuse-grs
